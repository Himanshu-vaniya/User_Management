export const API_ENDPOINTS = {
  LOGIN: '/api/auth/login',
  USERS: '/api/users',
  ROLES: '/api/roles',
  PERMISSIONS: '/api/permissions',
};

export const ROUTES = {
  DASHBOARD: '/dashboard',
  LOGIN: '/login',
};

export const STORAGE_KEYS = {
  TOKEN: 'auth_token',
  USER: 'auth_user',
};
