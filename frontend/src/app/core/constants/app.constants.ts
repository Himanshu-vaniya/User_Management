export const API_ENDPOINTS = {
  LOGIN: '/auth/login',
  USERS: '/users',
  ROLES: '/roles',
  PERMISSIONS: '/permissions',
};

export const ROUTES = {
  DASHBOARD: '/dashboard',
  LOGIN: '/login',
};

export const STORAGE_KEYS = {
  TOKEN: 'auth_token',
  USER: 'auth_user',
};
